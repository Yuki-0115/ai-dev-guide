# VibeOps × バイブコーディング【完成版】

## ― AI前提開発を"自走する仕組み"にする最善ガイド ―

---

## 0. このマニュアルの位置づけ

このガイドは、

* **CodeZine記事で語られていた「VibeOps（7割で運用に切り替える思想）」**
* **既存の『バイブコーディング実践ガイド』**

この2つを統合し、

> **「速く作る」ではなく「壊れずに回り続ける」AI前提開発プロセス**

として再設計した"最終版"です。

対象は以下：

* 個人開発〜小規模SaaS
* 属人化を嫌うチーム
* ジュニア主体 or 少人数開発
* 運用しながら育てるプロダクト（報告くん型）

---

## 1. 再定義｜バイブコーディングとは何か

### 従来定義（誤解されがち）

* AIにノリでコードを書かせる
* 爆速だが破綻しやすい

### 本定義（このガイド）

> **人間が"指揮者"となり、AIを並列稼働させ、
> 設計 → 実装 → 運用を一体化して回す開発様式**

ポイント：

* 書くのはAI
* 決めるのは人
* 守るのはルール

---

## 2. 全体構造｜VibeOps統合フロー（4フェーズ）

### フェーズ0：Spec固定（最重要）

**ここで8割決まる**

固定するもの（1枚でOK）：

* 機能要件（何ができるか）
* 非機能要件（速度・安全・拡張）
* 画面構成（最低限）
* データ構造（概念レベル）
* 命名規則 / デザインルール
* NG事項（やってはいけない）

👉 **このSpecは途中で変えない**

---

### フェーズ1：AI分業設計（並列化）

人間の役割：

* 作業を"工程"ではなく"部品"に分解

AIへの割り振り例：

* AI①：DB設計・モデル
* AI②：API設計
* AI③：UIコンポーネント
* AI④：テスト観点

ルール：

* 「全部作って」は禁止
* 必ず**部品単位**

---

### フェーズ2：7割完成で止める（重要）

ここが最大の思想転換。

❌ やらない：

* 完璧を目指す
* 全画面・全例外を潰す

⭕ やる：

* **主要ユースケースのみ完成させる**
* 不足・違和感はIssue化
* **OWASP Top 10観点でセキュリティレビュー**（後述）

> **完成ではなく"運用可能"をゴールにする**

#### セキュリティチェック（7割時点で必須）

```
このコードに対して、OWASP Top 10の各項目で
脆弱性がないかチェックして。
あれば具体的な攻撃シナリオと修正コードを出して。
```

チェック観点（最低限）：

* インジェクション（SQL / コマンド）
* 認証・認可の漏れ
* 機密データの露出
* 入力値バリデーション
* セッション管理

👉 **これを通過しないと運用フェーズに進まない**

---

### フェーズ3：運用＝開発（VibeOps）

* 不具合・改善・要望 → 全てIssue
* Issue単位でAIに再生成
* 修正 → レビュー → 即反映

特徴：

* 運用ログがそのまま開発ログ
* 仕様が"生き物"になる

#### 運用時のセキュリティ習慣

* 依存関係の定期チェック（`npm audit` / `pip-audit`）
* 新機能追加時は都度OWASPレビュー
* 本番障害時は「なぜ検知できなかったか」をIssue化

---

## 3. 上流を守る仕組み｜AI多数決ゲート

### なぜ必要か

* ジュニアが上流で詰まる
* 人レビューは属人化・遅延

### 解決策

**Spec / 要件はAIに承認させる**

### 仕組み（具体例）

1. 要件ドキュメント（Spec）を用意
2. **3つのLLMに同じプロンプトで投げる**
   - Claude
   - GPT-4
   - Gemini
3. 各AIに以下を聞く：

```
このSpecに対して、以下の観点で問題がないかレビューしてください：
- 機能要件の漏れ・矛盾
- 非機能要件の現実性
- セキュリティ上の懸念
- 実装時に曖昧になりそうな箇所

問題なければ「OK」、あれば具体的に指摘してください。
```

4. **2/3以上が「OK」または軽微な指摘のみ → 次フェーズへ**
5. 重大な指摘があれば修正して再投票

### 効果

* 上流品質の安定
* 人は判断疲れから解放
* 「誰かのせい」がなくなる

---

## 4. プロンプト運用ルール（実戦）

### 基本型

```
以下のSpecを厳守してください。
対象はこのIssueのみです。
変更範囲を明示し、既存コードは壊さないでください。
```

### 禁止ワード

* 「全部」
* 「最適化して」
* 「いい感じに」

### 推奨ワード

* 「この関数のみ」
* 「このIssueの再現条件を満たす」
* 「副作用が出ないように」

---

## 5. 役割分担（人間側）

人間がやることは3つだけ：

1. **決める**（Spec / 優先度）
2. **分ける**（Issue粒度）
3. **止める**（やりすぎ防止）

書かない。抱えない。属さない。

---

## 6. よくある失敗と回避

### 失敗①：AIに期待しすぎ

→ 解決：AIは作業員、設計者ではない

### 失敗②：Specが曖昧

→ 解決：曖昧なまま投げない

### 失敗③：完成を目指す

→ 解決：7割で運用へ

### 失敗④：セキュリティを後回し

→ 解決：7割時点で必ずOWASPレビュー

---

## 7. このガイドの本質

これは開発手法ではない。

> **「人が壊れないための構造設計」**

* 速さのためじゃない
* 楽するためでもない
* **続けるため**の設計

---

## 8. 次にやること（実践編）

### ① Specテンプレを作る

以下の項目を1ページにまとめる：

```markdown
# [プロダクト名] Spec

## 1. 概要
- 一言で何か：
- 誰のため：
- 解決する課題：

## 2. 機能要件
- [ ] 機能A：（具体的な動作）
- [ ] 機能B：（具体的な動作）
- [ ] 機能C：（具体的な動作）

## 3. 非機能要件
- 想定ユーザー数：
- 許容レスポンス時間：
- 可用性目標：

## 4. 画面構成
- 画面一覧（箇条書き or 簡易図）

## 5. データ構造
- 主要エンティティと関係（図 or 箇条書き）

## 6. 技術スタック
- フロント：
- バック：
- DB：
- インフラ：

## 7. 命名規則
- 変数：camelCase
- ファイル：kebab-case
- コンポーネント：PascalCase

## 8. NG事項（これはやらない）
- 
- 
```

### ② Issueテンプレを固定

```markdown
## 概要
（何が起きているか / 何をしたいか）

## 再現条件 or 完了条件
- 

## 影響範囲
- 

## 備考
- 
```

### ③ AI多数決レビューを導入

* Spec完成時に3 LLMでレビュー
* 2/3以上OKで着手

---

## 9. チェックリスト（運用開始前）

- [ ] Specテンプレ作成済み
- [ ] Issueテンプレ作成済み
- [ ] AI多数決の運用ルール決定
- [ ] OWASPレビュープロンプト準備
- [ ] 依存関係チェックの自動化設定

**全部チェックがついたら、開発は"イベント"ではなく"日常"になる。**

---

（完）
