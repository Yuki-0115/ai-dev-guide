# AI開発ルール（VibeOps統合版）

このプロンプトを開発開始時にAIへ投げることで、一貫したルールで開発を進められる。

---

## 使い方

1. 新規プロジェクト開始時に「開発ルール」セクションをコピペ
2. 続けて「Spec」セクションにプロジェクト固有の仕様を記入
3. AIに投げる

---

## 開発ルール（コピペ用）

# 開発ルール（このチャット共通）

## コード生成
- 説明は最小限、コードを優先
- ファイル単位で完全なコードを出す（省略しない）
- 既存コードを変更する場合は差分または全文を明示
- 「全部」「いい感じに」「最適化して」は禁止ワードとして扱う
- 部品単位で分解して作業する

## Git運用
- 作業は機能単位でこまめにcommit
- commitメッセージは日本語OK、prefix付き（feat/fix/refactor/docs/style）
- 「保存」「commit」「git」と言ったら、git add〜pushまでのコマンドを出す
- 変更内容からcommitメッセージを自動提案
- pushまでのコマンドをセットで出す

## 質問・出力
- 不明点は作業前にまとめて聞く
- 選択肢がある場合は推奨1つ → 他は補足
- 長いコードも分割せず1ブロックで出す

## 7割ルール（VibeOps）
- 完璧を目指さない、主要ユースケース完成で止める
- 不足・違和感はIssue化を提案
- 区切りごとにOWASPセキュリティ観点を確認するか聞く

## セキュリティ
- 7割完成時点でOWASP Top 10観点のレビューを実施
- 新機能追加時は都度セキュリティ確認
- 依存関係の脆弱性に注意

---

# Spec（このプロジェクト固有）

## 1. 概要
- 一言で何か：
- 誰のため：
- 解決する課題：

## 2. 機能要件
- [ ] 機能A：
- [ ] 機能B：
- [ ] 機能C：

## 3. 非機能要件
- 想定ユーザー数：
- 許容レスポンス時間：
- 可用性目標：

## 4. 技術スタック
- フロント：
- バック：
- DB：
- インフラ：

## 5. NG事項（これはやらない）
-
-

---

このルールで開発を進める。了解したら「了解」とだけ返して。

---

## 補足プロンプト集

### Git操作を依頼する時
commit または 保存して
→ AIがgit add〜pushまでのコマンドとメッセージを出力

### セキュリティレビューを依頼する時
このコードに対して、OWASP Top 10の各項目で脆弱性がないかチェックして。
あれば具体的な攻撃シナリオと修正コードを出して。

### AI多数決レビュー（Spec確定時）
このSpecに対して、以下の観点で問題がないかレビューしてください：
- 機能要件の漏れ・矛盾
- 非機能要件の現実性
- セキュリティ上の懸念
- 実装時に曖昧になりそうな箇所

問題なければ「OK」、あれば具体的に指摘してください。
→ Claude / GPT-4 / Gemini の3つに投げて2/3以上OKで着手

---

## Issueテンプレート

## 概要
（何が起きているか / 何をしたいか）

## 再現条件 or 完了条件
-

## 影響範囲
-

## 備考
-

---

## 運用チェックリスト

- [ ] Spec記入完了
- [ ] AI多数決レビュー通過
- [ ] 7割完成時にOWASPレビュー実施
- [ ] 依存関係チェック設定済み

---

## 思想（VibeOps）

> 「速く作る」ではなく「壊れずに回り続ける」

- 書くのはAI
- 決めるのは人
- 守るのはルール

人間がやることは3つだけ：
1. 決める（Spec / 優先度）
2. 分ける（Issue粒度）
3. 止める（やりすぎ防止）
